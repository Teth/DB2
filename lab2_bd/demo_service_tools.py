from subprocess import Popen, PIPE, STDOUT
from time import sleep

# Popen(["redis-server"], cwd=True)
# sleep(2)
# Popen(["python", "demo_journal.py"], shell=True)
# Popen(["python", "demo_worker_launch.py"], shell=True)
# Popen(["python", "demo_mock_client.py", "10"], shell=True)